{
  "name": "play-fest-bw",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && (cp *.html dist/ 2>/dev/null || true) && (cp *.js dist/ 2>/dev/null || true) && (cp *.cjs dist/ 2>/dev/null || true)"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
